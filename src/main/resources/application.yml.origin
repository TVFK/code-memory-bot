server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/memory_code_db
    username: username
    password: password
   liquibase:
     change-log: classpath:db/changelog/db.changelog-master.xml

bot:
  token: bot_token
  name: bot_name